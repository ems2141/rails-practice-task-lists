<%= form_for [@task_list, @task] do |f| %>

  <p>
  <%= f.text_area :description %>
  </p>

  <p>
  <%= f.select :year, [['January', 'january'], ['February', 'february'], ['March', 'march']] %>
  </p>

  <%= f.submit 'Create Task' %>

<% end %>